# Google Gemini API Key (Server-side only)
# Get your API key from: https://aistudio.google.com/app/apikey
# This key is required for AI-powered features (business name generator, digital strategy, AI agent)
# IMPORTANT: Set this as GEMINI_API_KEY in Netlify environment variables (not as VITE_* to keep it server-side only)
GEMINI_API_KEY=your-google-gemini-api-key-here
